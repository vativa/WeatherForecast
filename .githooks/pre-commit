#!/bin/sh

branch="$(git symbolic-ref --quiet --short HEAD 2>/dev/null || true)"

if [ "$branch" = "main" ]; then
  echo "✗ Direct commits to 'main' are blocked"
  echo "✗ Create a feature branch from origin/main and commit there"
  exit 1
fi

