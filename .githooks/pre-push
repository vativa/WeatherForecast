#!/bin/sh

branch="$(git symbolic-ref --quiet --short HEAD 2>/dev/null || true)"

if [ "$branch" = "main" ]; then
  echo "✗ Pushing from 'main' is blocked"
  echo "✗ Push a feature branch and open a PR instead"
  exit 1
fi

